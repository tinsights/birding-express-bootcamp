<%- include('partials/header') %>
<div class="container-fluid">
  <div class="container">
    <table class="table table-hover table-striped">
      <thead class="table-secondary">
        <tr>
          <th scope="col">No.</th>
          <th scope="col">Summary</th>
        </tr>
      </thead>
      <tbody>
        <% allNotes.forEach((note) => { %>
        <tr>
          <th scope="row"><%= note.id %></th>
          <td><a href="<%= `/note/${note.id}` %> "> <%= note.summary %> </a></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    <nav aria-label="Page navigation example">
      <ul class="pagination">
        <li class="page-item"><a class="page-link" href="<%= `/${Number(page)-1}` %> ">Previous</a></li>
        <li class="page-item"><a class="page-link" href="<%= `/${Number(page)+1}` %>">Next</a></li>
      </ul>
    </nav>
  </div>
  </form>
</div>
<%- include('partials/footer') %>
